spring:
  application:
    name: ALUNO_SERVICE_TEST
  datasource:
    url: jdbc:postgresql://localhost:5432/escola_db_homol
    username: admin
    password: admin1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
server:
  port: 8082
