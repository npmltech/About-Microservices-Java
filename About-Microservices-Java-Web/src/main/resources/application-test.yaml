spring:
  application:
    name: ALUNO-SERVICE-TEST
  datasource:
    url: jdbc:postgresql://localhost:5432/escola_db_homol
    username: admin
    password: admin1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
server:
  port: 8087
